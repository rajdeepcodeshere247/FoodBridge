// Auth controller — logic for login, logout, session
// const pool = require('../config/db.config');

const getMe = async (req, res) => {
  // TODO: return current user from req.user (set by passport)
};

module.exports = { getMe };
